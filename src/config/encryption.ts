// Local encryption key for SQLite field encryption at rest.
// Production apps should store this in secure hardware (e.g. Keychain).
export const DB_ENCRYPTION_KEY = 'AutoExpense-CW2-Local-Key-2026';
